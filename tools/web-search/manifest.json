{
  "id": "web-search",
  "name": "Web Search",
  "description": "Search the web using the Tavily API.",
  "version": "1.0.0",
  "credentials": [
    {
      "name": "api_key",
      "label": "Tavily API Key",
      "description": "API key from tavily.com",
      "required": true
    }
  ],
  "functions": [
    {
      "name": "search",
      "description": "Search the web for a query and return relevant results with titles, URLs, and content snippets.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query."
          },
          "max_results": {
            "type": "number",
            "description": "Maximum number of results to return. Defaults to 5."
          }
        },
        "required": ["query"]
      }
    }
  ]
}
